image: gradle:latest

stages:
  - build

build:
  stage: build
  script: gradle clean build --continue
  artifacts:
    when: always
    reports:
      junit:
        - "*/build/test-results/test/TEST-*.xml"
