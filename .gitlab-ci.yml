image: gradle:latest

stages:
  - build

build:
  stage: build
  script: gradle clean build --continue
  coverage: /Method Coverage.*?([0-9]{1,3})%/
  artifacts:
    when: always
    reports:
      junit:
        - "*/build/test-results/test/TEST-*.xml"
